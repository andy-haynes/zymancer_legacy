import {
  ImportRecipe
} from '../constants/RecipeActionTypes';

const savedRecipes = (state = [], action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default savedRecipes;