import {
  ImportRecipe
} from '../constants/RecipeActionTypes';

const fermentation = (state = [], action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default fermentation;